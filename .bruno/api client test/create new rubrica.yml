info:
  name: create new rubrica
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5555/api/rubrica
  body:
    type: json
    data: |-
      {
        "name": "Luigi",
        "surname": "Gialli",
        "email": "luigi.gialli@example.com",
        "telephone": "1234567890"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
