info:
  name: search rubrica
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5555/api/rubrica/search?email=mario.rossi@example.com
  params:
    - name: email
      value: mario.rossi@example.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​