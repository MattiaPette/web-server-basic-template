# web-template (TypeScript)

Quick TypeScript setup for this repository.

Commands:

- Install deps:

```bash
npm install
```

- Development (auto-restart):

```bash
npm run dev
```

- Build:

```bash
npm run build
```

- Run built app:

```bash
npm start
```
